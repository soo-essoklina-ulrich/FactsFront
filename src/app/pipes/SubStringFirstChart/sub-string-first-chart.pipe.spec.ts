import { SubStringFirstChartPipe } from './sub-string-first-chart.pipe';

describe('SubStringFirstChartPipe', () => {
  it('create an instance', () => {
    const pipe = new SubStringFirstChartPipe();
    expect(pipe).toBeTruthy();
  });
});
